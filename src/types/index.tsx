export interface IProducts {
    id: number;
    name: string;
    price: number;
    desc: string;
    img: string;
}

export interface Iuser {
    email: string;
    password: string;
    id: number;
    role: number;
}